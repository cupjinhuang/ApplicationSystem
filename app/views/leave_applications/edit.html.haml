.page-header
  %h1
    = t(:editing)
    = t('activerecord.models.leave_application')

= render 'form'

= link_to t(:show), @leave_application, class: 'btn'
= link_to t(:back), leave_applications_path, class: 'btn'
